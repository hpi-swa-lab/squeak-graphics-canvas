a GLGenerator generates the OpenGL interface from an XML document.
To re-generate the interface do the following:

Drop XML file into squeak window

open inspector 

GLGenerator generate: self api: 'gl'